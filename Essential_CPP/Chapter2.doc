2.1 如何编写函数
	返回类型 函数名 参数列表 
      	  {
           函数体
           }
2.2 调用函数
 	传值 利用局部变量复制参数，不改变原有值。不可返回局部变量的引用和地址
	传址 reference/pointer 利用应用来传递参数，可以返回引用或指针 
　　Int val = 1024;
　　Int *pi = &val;
　　Int &rval = val：
　　注意:不管是何种方式，对于函数内部申请的局部变量，不可返回局部变量的引用和地址 ，只可以返回值，因为局部变量地址/应用过了函数周期无效。 
